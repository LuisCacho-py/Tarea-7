name: Work 1 Cacho

on: [push]

jobs: 
  build:
    runs-on: ubuntu-latest
    container: python:3.10
    steps:
      - run: echo "TESTING"
      - run: python --version
      - run: pip --version
      - uses: actions/checkout@v3
      - run: python -c "print('Hola Mundo')"